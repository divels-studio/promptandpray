# dev/backlogs/archive

Finished plans of the plugin's own development land here.

The history of the plugin's EXTRACTION (the PROMPTANDPRAY mission, v0.1.0) is not here: it was
run from the project the loop was extracted from and lives in that repository's archive as
`docs/backlogs/archive/009_PLAN_PROMPTANDPRAY_2026-08-29.md` (machine-local, single operator).
Nothing from that archive is copied into this repository - it is a pointer, not a mirror.
