# scripts/selfcheck/

The self-check engine: asserts the installed project layer against the config
(owned permission rules, wiring, version/hash bookkeeping invariants). Final
step of both setup and update.

Populated in P1 (rewrite) and integrated in P4. Empty by design at P0.
