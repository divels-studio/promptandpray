# scripts/setup/

The installer engine: interview + generate (project config, agents, roles,
overrides doc, managed CLAUDE.md region, permission rules, hash bookkeeping).

Populated in P2. Empty by design at P0.
