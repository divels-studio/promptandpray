# What changed: 0.2.4 -> 0.2.5

This report is generated by `/pnp:update`. It is a one-off note to you, not tracked bookkeeping:
each update writes its own version-named report; keeping or deleting them is your call, nothing reads them back.

An unheld artifact you had not edited, whose payload render changed, was applied without a dialog; edited ones were asked about; held ones were recorded, not applied.

## Review these sections

- **verdict-report-and-one-word-per-pass** - 0.2.5 changes two things the loop DOES, and both of them travel with /plugin update rather than with this migration: the doctrine your session reads is the payload's own docs/ and skills/, which are replaced wholesale by the plugin update. First, a Reviewer or QA verdict now reaches you as the verdict plus one or two sentences of its substance, before the next dispatch step - never a bare 'pass, moving on', which is how an audit you paid for used to disappear on the way to the next dispatch. Second, and this one costs money in the other direction: the default that let the COO dispatch a later auditor pass on the word you gave for the ticket is REVOKED. Pass 1 of a route rides the ticket's word; every further auditor pass - a further configured readiness pass, the verification pass after a correction round that touched code, a pass beyond the review contract, a round past the correction cap - is dispatched only after you say so, one word per pass. `review.<class>.passes` is from now on a CEILING, not a budget the COO may spend on its own; nothing about the numbers in your audit table changes, only who releases each pass after the first. The one operation that touches your project is the second one below: the managed `aiwf-core` region of your CLAUDE.md carries the operator-gates paragraph, and that paragraph stated the revoked default in one line. It is re-rendered here. Your own text outside the markers is untouched, and if you hold that region through an override it is not re-applied at all - the new render is recorded as upstream and the CHANGES report says so.
  - CHANGELOG.md

## Applied

### 0009_readiness-discipline (-> 0.2.5)

- `note` verdict-report-and-one-word-per-pass
- `rerender-managed-region` CLAUDE.md#aiwf-core - payload-current

Nothing here was committed: the diff is yours to review and commit as usual.
