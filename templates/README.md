# templates/

Rendered-into-the-project artifacts: the managed CLAUDE.md region, agent files,
the project overrides document, roles, the permission ask-ruleset, memory seeds.

Populated in P1-P2. Empty by design at P0.
