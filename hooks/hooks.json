{
  "description": "PromptAndPray enforcement gates: Gate 1 (non-writer subagents cannot write) and Gate 2 (the Writer dispatch gate - a click on every dispatch, or only on one the active PLANs cannot account for, per enforcement.dispatchGate).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/engine/pretooluse-mutation-guard.js\""
          }
        ]
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/engine/pretooluse-dispatch-gate.js\""
          }
        ]
      }
    ]
  }
}
