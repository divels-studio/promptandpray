# What changed: 0.2.0 -> 0.2.1

This report is generated by `/pnp:update`. It is a one-off note to you, not tracked bookkeeping:
each update writes its own version-named report; keeping or deleting them is your call, nothing reads them back.

An unheld artifact you had not edited, whose payload render changed, was applied without a dialog; edited ones were asked about; held ones were recorded, not applied.

## Review these sections

- **posix-legs** - 0.2.1 is a code-only release: the defects that kept the Linux and macOS CI legs red are fixed, the six CLI entrypoints now recognise themselves when the payload sits behind a symlinked directory (a POSIX temp dir is one), and the self-check's provenance scan no longer reads the harness's own plugin-cache markers as payload files. All of it is payload code - this migration touches nothing in your project: no config key, no managed region, no agent file. There is nothing for you to do; the CHANGELOG entry is the whole story
  - CHANGELOG.md

## Applied

### 0005_posix-legs (-> 0.2.1)

- `note` posix-legs

Nothing here was committed: the diff is yours to review and commit as usual.
