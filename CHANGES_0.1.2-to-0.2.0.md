# What changed: 0.1.2 -> 0.2.0

This report is generated by `/pnp:update`. It is a one-off note to you, not tracked bookkeeping:
each update writes its own version-named report; keeping or deleting them is your call, nothing reads them back.

An unheld artifact you had not edited, whose payload render changed, was applied without a dialog; edited ones were asked about; held ones were recorded, not applied.

## Review these sections

- **overrides-loop-shape** - Your overrides document's Loop shape section still says plan readiness has two passes; the contract is now `review.plan.passes` (`/pnp:roles`) - edit that line yourself, the document is yours
  - docs/WORKFLOW.md

## Applied

### 0004_audit-table (-> 0.2.0)

- `add-config-key` review.plan
- `add-config-key` review.code
- `add-config-key` review.docs
- `rerender-managed-region` .claude/aiwf-native/roles.json - payload-current
- `rerender-managed-region` .claude/agents/reviewer.md - not on this installation - skipped
- `note` overrides-loop-shape

Nothing here was committed: the diff is yours to review and commit as usual.
