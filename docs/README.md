# docs/

The generic doctrine shipped as plugin payload: the workflow (routing, roles,
brief contract, gates), the one-page loop mechanics, the review checklist, the
operator protocol and the recipes. Project-specific direction never lives here -
it lives in the generated project overrides document.

Populated in P1. Empty by design at P0.
